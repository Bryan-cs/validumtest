[deploy]
startCommand = "alembic upgrade head && uvicorn main:app --host 0.0.0.0 --port $PORT --workers 2 --loop uvloop --log-config log_config.json"
healthcheckPath = "/health"
healthcheckTimeout = 60
region = "us-east4"
