[deploy]
startCommand = "if [ -n \"$PORT\" ]; then alembic upgrade head && uvicorn main:app --host 0.0.0.0 --port $PORT --workers 1 --loop uvloop --log-config log_config.json; else python run_daily.py; fi"
healthcheckPath = "/health"
healthcheckTimeout = 60
region = "us-east4"
